# data/processed

Generated, respondent-level working data produced by notebooks/scripts from the files in `data/raw`.

- Contents here are derived and reproducible — regenerate from `data/raw` rather than editing by hand.
- Not committed to Git (see `.gitignore`).
