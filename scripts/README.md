# scripts

Reusable data preparation and export scripts (e.g. cleaning `data/raw` into `data/processed`, or exporting aggregates into `public/data`).

Currently empty — scripts will be added once the analysis in `analysis/` stabilizes.
